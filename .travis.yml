sudo: false
language: python
python:
    - 3.6
    - 3.7
    - 3.8

before_install:
    - sudo apt-get update

install:
    - pip install -r requirements.txt
    - pip install -r requirements-dev.txt

script:
    - coverage run commands.py init en_US
    - coverage run commands.py update
    - coverage run commands.py compile
    - coverage run commands.py gen-plugin-info
    - coverage run test.py {}

after_success:
    - codecov
